node_modules/
dist/
*.log
.DS_Store
.agents/
scratch/
.artifacts/
.tmp/
apps/electron/.artifacts/
apps/electron/.tmp/
apps/electron/release/
# 更新闭环演练产物：两个临时版本的完整安装包，只在本地跑 verify-update-flow 时存在
apps/electron/verify-update/
docs/*-debug.png
docs/*-result.png

# 内部文档：留在本地，不进公开仓库。
# 这四份是给自己看的（架构底稿、词汇表、产品定位、Agent 约定），不是给使用者看的；
# 已从索引删除，此处确保它们不会被下一次 `git add -A` 又带回去。
AGENTS.md
CONTEXT.md
DESIGN.md
PRODUCT.md

# 跑批原始导出：含原文与译文正文（版权 + 工作区数据），只留本地
runs/

# 文档一律留在本地，不进仓库：初版只发源码与 Release。
# docs/ 下也不得存在任何构建输入——界面外壳的源文件在
# apps/electron/renderer/shell/，那里才是源码该待的地方。
.scratch/
docs/

# 版权素材：真实出版物不进仓库（测试自建 EPUB，不依赖它）
tests/*.epub

# wiring 文档是 wiring:generate 的产物
apps/electron/wiring/electron-wiring.html

# 作者私人的人译对照材料（真实作品译文，绝不入库）
packages/engine/fixtures/reference/
